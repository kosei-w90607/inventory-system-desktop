# Test Design Matrix: UI 一覧の背骨 D — Lane 4（識別列固定 / ページ送りの上下切り分け / 表示件数の位置統一 / 枠の地色）

Plan Packet: [../2026-09-05-ui-list-backbone-d-lane4.md](../2026-09-05-ui-list-backbone-d-lane4.md)

Plan Review round 1（Opus）が reject した旧 SC1（`ListShell` wrapper 撤去）・旧 SC2b〜SC2f（7 画面中 6 画面の長文列/table 幅変更）は撤回済み。round 2（Opus、reject）で在庫照会の上部 summary 条件・整合性チェックの frame 除外・table wrapper の rounded-lg 統一・範囲外 page の検知漏れを是正した（第 1 便）。owner 決定（2026-09-05、第 2 便）で item (1) を D（表を縮ませる）から識別列固定（Excel 型、`<main>` 基準 sticky-left、商品一覧のみ）へ書き換え、旧 SC-PT（部門列 `whitespace-normal`）を撤回し SC9（識別列固定）へ置き換えた。**owner Windows native L3 run 1（PR #40、2026-09-06）で AC-L3-1 が FAIL、owner 決定 案 X により item (1) は `<main>` 基準 sticky-left から data grid 型 scroll 箱へさらに書き換え**（Gated Amendment 1、GA1a〜GA1d）。同 run で AC-L3-3（一括価格改定の取引先追加ボタン位置）も所感あり（Gated Amendment 2、GA2）。SC9 自体（`identityColumns` の class 契約）は Gated Amendment 1 でも不変——変わるのは ListShell の外側の構造（scroll 箱で包む）のみ。**owner L3 run 2（2026-09-06〜07）**: AC-L3-1（縦）/AC-L3-3 は PASS、FAIL 1 件（最狭幅 × 2 ページ目以降で部門列が固定列の下へ隠れる）を owner 実機観測で機序確定し GA3a（商品コード列固定幅 + sticky offset 一致）を追加。設計所感（owner 決定 (a)）を受け GA3b（箱の高さ方針を行数基準 `max-h` へ試作、GA1 の縦方向 flex 高さ連鎖を撤去）を追加。SC9/GA1a〜GA1d/GA2 の契約は GA3a では不変、GA3b では GA1a/GA1b の高さ連鎖 class 契約が反転する（下記 GA3b-1〜GA3b-5 参照）。

## Risk

R3（商品一覧の識別列固定・scroll 箱構造・table overflow 挙動 + 共有 component `Pagination`/`PaginationSummary` の変更 + operator workflow の見た目変更）。狭幅での識別列固定・scroll 箱の実際の見え方は happy-dom のレイアウト計算では判定できないため、AC-L3-1（窓を狭める実機確認、商品一覧 1 画面、run 2）が oracle。本 Matrix は DOM の class/属性契約・条件分岐（render/no-render）・DOM 順に限定する。**round 3 是正（Opus P1）**: toolbar frame への sticky 追加は no-op と判明したため Scope から撤回。**Gated Amendment 1（owner L3 run 1、2026-09-06）**: `<main>` 基準 sticky-left モデルを data grid 型 scroll 箱へ置換、GA1a〜GA1d を追加。**round 2 是正（Sonnet P2、2026-09-06）**: GA1e（`stickyHeader`/page root 高さ連鎖 fs scan の regression guard）を追加。**Lane 4 Gated Amendment 3（owner L3 run 2 実機観測、2026-09-06〜07）**: GA3a（商品コード列固定幅 + sticky offset 一致、`ProductTable.tsx`/`ListShell.tsx`）と GA3b（箱の高さ方針試作、GA1a/GA1b/GA1e の高さ連鎖 class 契約を反転し scroll 復元を main/箱 二元化、`ProductListPage.tsx`/`ListShell.tsx`/`page-scroll.ts`/`app-router.ts`）を追加。GA3b は視覚的な trade-off（page scroll 中の thead 挙動）を含むため AC-L3-4（run 3）を判定材料として置き、本 Matrix は class/DOM/scroll 値の機械検査に限定する。

## Contracts Under Test

- SC9: `ListShell.tsx` の `identityColumns` prop を活性化すると、root の className（descendant variant 文字列）に先頭 2 列（`thead th`/`tbody td`）向けの `nth-child(1)`/`nth-child(2)` selector を含む sticky/left/背景/z-index/境界 class が含まれる（`tbody` は `bg-background`/`z-[1]`/右端 shadow + `forced-colors:border-r`、`thead th` は既存 `top`/`z-10` に加え `z-[11]`）。`identityColumns` 未設定では root にこれらの class が含まれない。`ListShell.tsx` 自体は既存 wrapper（`:141` `w-min min-w-full`）・sticky-top 指定を無変更のまま。`ProductListPage.tsx` は `identityColumns={2}` を渡し、`ProductTable.tsx` 商品コード列に `w-28` が付く（他の列 class は無変更）。**round 3 是正（Opus P6）**: `ListShell.tsx` は `STICKY_TABLE_CLASSES` を実 DOM の `thead th`/`tbody td` へ直接 render するのではなく root の descendant variant class として持つ（`ListShell.tsx:5-6` の設計方針、`ListShell.test.tsx:313-319` の `classTokens(container.firstElementChild)` が既存の検査 precedent）。したがって SC9a/b は rendered `th`/`td` の classList ではなく **root の class token** を対象に「含む/含まない」を検査する
- SC1a〜SC1c: `Pagination`（下部）が `totalPages <= 1`（`totalCount === 0` 含む）のとき何も描画せず、`totalPages > 1` では従来どおり描画し、51 件/perPage 50 の 2 ページ目で「前へ」が有効
- SC2: `PaginationSummary` が新規に `text-sm text-muted-foreground tabular-nums` を持つ（`text-base text-foreground` 撤去、下部の分割表記とは別に 1 箇所だけ新規出現）
- SC3a〜SC3g: 7 画面（`ListShell` を使わない全画面）で `totalCount > 0` の結果表示時に上部 `PaginationSummary` が新規描画され、既存の単数一致 `getByText` 重複文言 test 3 file 6 箇所が是正される。**SC3b（在庫照会）のみ round 2 是正**: 下部と同じ `statusValue === "all" && totalCount !== null` の 2 条件でのみ描画（`totalCount: number | null`、`types.ts:54`）
- SC4a〜SC4f: 6 画面の枠が `rounded-lg border bg-card p-4`（`bg-stone-50` / 枠なし / `bg-card` 欠落からの是正）。整合性チェックは round 2 是正で対象外（`IntegrityCheckPage.tsx:234` は Select 単独ラッパーで filter フィールドを持たない）
- SC4g: `IntegrityCheckPage.tsx:348`/`OperationLogsPage.tsx:496` の table wrapper が `overflow-x-auto rounded-md border` → `overflow-x-auto rounded-lg border` へ（round 2 是正、`bg-card` は付けない）
- SC5a〜SC5b: `StocktakePage` と一括価格改定（`PriceRevisionFilters`）で表示件数 `Select` が枠内の最後尾（他要素の後）に描画される。既存 SC10/SC9a の rewrite・pass 維持が oracle
- SC6: `StocktakePage.tsx:854` の `<fieldset>` が単一ページで描画されない
- SC7: `ListShell.test.tsx` の既存 it は `:346` の wrapper 完全一致を含め、S2 の `text-base` 判別子書換え（`:96,98,107,119`）以外は無変更のまま pass する（回帰確認。本 lane は `ListShell.tsx` を S9 の範囲〈identityColumns〉でのみ変更する）
- SC8（round 2 是正、AC13。round 3 で行番号を再確認）: フィルタ変更で `totalCount` が perPage 未満へ減っても `page > totalPages` の状態で描画される画面が無い（`StockInquiryPage.tsx:194`/`OperationLogsPage.tsx:449` の既存「先頭ページに戻る」`EmptyState` 以外の画面から 1 画面選定）
- GA3a-1〜GA3a-4（Lane 4 Gated Amendment 3、owner L3 run 2 実機観測。round 1 是正 Opus P1-2/P3 で td/th 自体への `min-w`/`max-w` 依存から div wrapper 方式へ変更）: `ProductTable.tsx` 商品コード td 内の div が `w-32`/`whitespace-normal`/`break-all` を持ち、th 内の div が `w-32` を持つ（td/th 自体は belt の `min-w-36` のみ、`max-w-36` は付けない）；`ListShell.tsx` `IDENTITY_COLUMN_CLASSES[2]` の root token が `left-[9rem]`（`thead th:nth-child(2)`/`tbody td:nth-child(2)` 双方）を含み `left-[7rem]` を含まない（`src/` 全体で `left-[7rem]` = 0、`ProductListPage.test.tsx:667` を含む 3rd site も対象）；`PRODUCT_CODE_CELL_WRAPPER_WIDTH_CLASS`（`w-32` = 8rem + td/th padding 1rem = 9rem）と `left-[9rem]` から抽出した rem 値が一致する（parity oracle）
- GA3b-1〜GA3b-5（Lane 4 Gated Amendment 3、箱の高さ方針試作）: `ProductListPage.tsx` の `PageShell` root が高さ連鎖 class を持たない（GA1a の逆転）；`ListShell.tsx` stickyHeader root が `flex`/`flex-1`/`min-h-0`/`flex-col` を持たない（GA1b の逆転）；箱要素が `max-h-[168rem]`（最終決定値）/`overflow-auto` を持ち `min-h-[12rem]`/`flex-1` を持たない；toolbar が `shrink-0` を持たない；`page-root-pageshell-sweep.test.ts` の GA1e が反転されている（削除ではない）
- GA3b-6〜GA3b-8（scroll 復元の main/箱 二元化、round 1 是正 Opus P2）: `<main>`（縦、51 行以下）と箱（横は常に、51 行超のときは縦も）が独立に復元される（両方を同時に scroll した状態からの同時復元を含む）；forced-top（`app-router.ts:92-95`/`:52-58`）が main（縦）と箱（横 + 51 行超時は縦）の両方をリセットする；`scrollPageToTop()`（`page-scroll.ts:50-59`）が `??` 択一ではなく main と箱の両方へ `scrollTo` を呼ぶ

## Failure Modes

- `identityColumns` を渡しても root の class token に先頭 2 列向けの固定 selector class が含まれない、または `identityColumns` を渡さない画面の root にこれらが誤って含まれる
- 固定 class が誤った列数（1 列だけ、または 3 列目以降にも）に適用される
- 識別列の `thead th` に `z-[11]` が付かず、他の非識別列 `th` と同じ `z-10` のまま重なりが不定になる（round 3 追加）
- `ListShell.tsx:141` の `w-min min-w-full` wrapper・`overflow-visible`・sticky-top 指定が変更されてしまう
- 商品一覧以外の 7 画面に `identityColumns` の配線・table 幅・長文列の変更が混入する（Non-scope 逸脱）
- `Pagination` が `totalPages <= 1` でもボタン・文言を描画してしまう、または `totalPages > 1` なのに誤って非表示になる
- 上部 `PaginationSummary` が 7 画面のいずれかで描画されない、`totalCount === 0` 相当のタイミングで誤って描画される、または新設に伴う重複文言で既存 test が壊れたまま放置される
- 枠色が `bg-card` にならない画面が残る、または `bg-card` 追加で page 内の他要素（`StockMovementsPage.tsx:98` の商品情報 card 等、Non-scope）を誤って巻き込む
- 表示件数 `Select` が枠内の最後尾に来ない（`StocktakePage` の reorder 漏れ、`PriceRevisionFilters` への配線漏れ、既存 SC9a が検出する `onPerPageChange` 未配線）
- `StocktakePage.tsx:854` の `<fieldset>` が単一ページで空要素のまま残る
- `ListShell.tsx` に意図しない変更が混入し `ListShell.test.tsx` の既存 it のいずれかが壊れる（特に `:346` の wrapper 完全一致）
- 在庫照会の上部 summary が下部と異なる条件（`totalCount>0` のみ等）で描画され、`source: "low_stock"` のとき誤って表示される
- 整合性チェックの Select 単独ラッパーに誤って `bg-card` frame が付く、または table wrapper の `rounded-md`→`rounded-lg` 統一漏れ
- フィルタ変更で `totalCount` が減った直後に `page > totalPages` のまま一覧が描画される画面が新たに生まれる
- GA3a: 商品コード td/th 内 div の `w-32`/`whitespace-normal`/`break-all` のいずれかが欠落する、offset（`left-[9rem]`）だけが変更され div 幅 + padding と不一致になる、`w-28` 消滅チェックが `ProductTable.tsx` 以外の 4 file（`DisposalPage.tsx`/`InventoryRecordsPage.tsx`/`ReceivingPage.tsx`/`IntegrityCheckPage.tsx`）の正当な `w-28` を誤検出する、`left-[7rem]` の是正が `ListShell.tsx` のみに留まり `ProductListPage.test.tsx:667` の 3rd site が見逃される
- GA3b: 高さ連鎖 class（root/toolbar/箱のいずれか）が反転漏れで残存する、箱の `max-h` が欠落し無制限に伸びる（51 行超でも scroll しない）、main の縦復元だけ実装され箱の横復元が落ちる（またはその逆）、main/箱を同時に scroll したケースで disarm の合成漏れにより片方しか復元されない、forced-top が main/箱の一方にしか効かない、`scrollPageToTop()` が `??` 択一のまま片方にしか効かない、`page-root-pageshell-sweep.test.ts` の GA1e が反転漏れで red のまま残る

## Test Matrix

| Contract | Failure Mode | Test Type | Test Name | Would fail if... |
|---|---|---|---|---|
| SC9a ListShell 識別列 class（あり、root token） | 固定 class 欠落 / 誤った列数 | unit（既存 `ListShell.test.tsx` 拡張、`classTokens(container.firstElementChild)` で root を検査、`:313-319` precedent 踏襲） | SC9a: with identityColumns={2}, root class tokens contain nth-child(1)/nth-child(2) selectors for thead th/tbody td with sticky+left-0/left-[7rem] (+ tbody: bg-background, z-[1], right-edge shadow, forced-colors:border-r; thead th: z-[11]); no nth-child(3) or later selector variant is present | root token に列 1-2 向け selector が欠落する、または列 3 以降向けの selector が誤って含まれる、または `thead th` に `z-[11]` が無い |
| SC9b ListShell 識別列 class（なし、対照、root token） | 誤爆 | unit（既存 `ListShell.test.tsx` 拡張、対照 case） | SC9b: without identityColumns, root class tokens contain none of the identity-specific tokens (nth-child selectors for columns 1-2, z-[11], forced-colors:border-r on tbody) | `identityColumns` 未設定でも識別列固有の token が root に含まれる |
| SC9c ListShell wrapper 不変（round 3 是正、toolbar sticky は撤回） | wrapper 変更 | unit（既存 `ListShell.test.tsx` 拡張） | SC9c: `:141` wrapper className stays exactly `w-min min-w-full` after identityColumns is activated | wrapper の class が変わる |
| SC9d ProductListPage/ProductTable 配線（round 3 是正、rendered root 経由で確認） | 未配線 | unit（`ProductListPage.test.tsx`: render 済み `ListShell` root の class token で `identityColumns={2}` 相当の適用を確認、mock された prop capture ではない。`ProductTable.test.tsx` 拡張） | SC9d: ProductListPage's rendered ListShell root carries the identityColumns=2 class tokens; ProductTable's 商品コード cell has w-28 | root token に識別列 class が反映されない、または `w-28` が付かない |
| SC1a Pagination 単一ページ非表示 | ボタン/文言残存 | unit（既存 `Pagination.test.tsx` 拡張） | SC1a: totalPages<=1 (including totalCount===0) renders null | `container.firstChild` が null にならない |
| SC1b Pagination 複数ページ表示 | 過剰適用（空集合 oracle 対） | unit（同上、対照 case） | SC1b: totalPages>1 still renders summary + 前へ/次へ | totalPages>1 なのに null になる |
| SC1c 51件/50件表示の 2 ページ目 edge | 「前へ」欠落 | unit（既存 `Pagination.test.tsx` 拡張、edge oracle） | SC1c: totalCount=51, perPage=50, page=2 renders an enabled 前へ button | 「前へ」が disabled または非表示になる |
| SC2 PaginationSummary style | 旧 class 残存 / oracle 誤り防止 | unit（既存 `Pagination.test.tsx` 拡張） | SC2: PaginationSummary root has exactly text-sm text-muted-foreground tabular-nums (new, single occurrence), text-base text-foreground has 0 occurrences | 旧 class が残る、新 class が付かない、または下部の分割表記と誤って同一視される |
| SC3a〜SC3g 7 画面の上部 summary 新規描画 + 既存重複文言 test 是正 | 描画漏れ / 既存 test 破綻放置 | unit（各 Page.test.tsx 拡張、7 file。うち `IntegrityCheckPage.test.tsx`/`OperationLogsPage.test.tsx`/`StockInquiryPage.test.tsx` は既存 `getByText` 是正を含む） | SC3x: results view renders PaginationSummary text above the table when totalCount > 0; existing single-match getByText assertions at IntegrityCheckPage.test.tsx:414, OperationLogsPage.test.tsx:280/292/419/431, StockInquiryPage.test.tsx:526 are rewritten to getAllByText/within and still pass | 対象画面のいずれかで上部 summary が見つからない、または上記 6 箇所のいずれかが多重一致で例外を投げたまま残る |
| SC3b 在庫照会 上部 summary 条件（round 2/3 是正） | 上部が誤って常時描画される、下部と条件が食い違う、または 0 件で描画される | unit（既存 `StockInquiryPage.test.tsx` 拡張） | SC3b: top PaginationSummary renders only when statusValue === "all" && (totalCount ?? 0) > 0, matching the corrected bottom Pagination gate | status が `"all"` 以外、`totalCount===null`、または `totalCount===0` で上部が描画される、または条件成立時に描画されない |
| SC4a〜SC4f 6 画面の枠 | 旧枠残存/新枠欠落 | unit（各 Page/Component.test.tsx 拡張、file 別 `rg` anchor は Plan Packet Scope S1 参照） | SC4x: filter section root has rounded-lg border bg-card p-4 | 旧 class（`bg-stone-50`／枠なし／`bg-card` 欠落）が残る |
| SC4g table wrapper rounded-lg 統一（round 2 是正） | rounded-md 残存 / bg-card 誤付与 | unit（`IntegrityCheckPage.test.tsx`/`OperationLogsPage.test.tsx` 拡張） | SC4g: table wrapper div has overflow-x-auto rounded-lg border, no bg-card | `rounded-md` が残る、または `bg-card` が誤って付く |
| SC5a StocktakePage Select 最後尾（既存 SC10 rewrite） | 順序不変 | unit（既存 `StocktakePage.test.tsx:1055` の SC10 rewrite、新規 test ではない） | SC10（rewrite）: filter row lists department filter, then uncounted-only checkbox, then per-page select in that DOM order | DOM 順で Select が Checkbox より前にある |
| SC5b PriceRevisionFilters Select 配線（既存 SC9a が oracle） | 未配線残存 | unit（既存 `PriceRevisionPage.test.tsx:694-703`（re-stack 後の行番号）の SC9a、無変更のまま pass 必須 + `PriceRevisionFilters` 側の位置 assertion 追加） | SC9a（無変更）+ 新規: 表示件数 Select renders inside PriceRevisionFilters as the last child of the filter row, and clicking it still calls mockScrollPageToTop | SC9a が fail する（`onPerPageChange` 配線漏れ）、または Select が `PriceRevisionPage.tsx` 側に独立して残る |
| SC6 StocktakePage fieldset ガード | 空 fieldset 残存 | unit（既存 `StocktakePage.test.tsx` 拡張） | SC6: fieldset wrapping the bottom Pagination is absent when totalPages<=1 | 単一ページで空の `<fieldset>` が DOM に残る |
| SC7 ListShell 回帰 | 意図しない副作用 | unit（既存 `ListShell.test.tsx`、S2 起因の判別子書換え 4 箇所以外は無変更 pass 必須） | SC7: existing it (including :346 wrapper exact-match) still pass unmodified | 本 lane の変更が `ListShell.tsx` に混入し既存 it のいずれかを fail させる |
| SC8 範囲外 page 非到達（round 2 是正、AC13） | page > totalPages のまま描画される | unit（1 画面、既存 test 拡張。フィルタ変更で totalCount を減らして page reset を確認） | SC8: changing a filter that shrinks totalCount below the current page's range still leaves page <= totalPages (via existing reset handler) | フィルタ変更後も `page > totalPages` の状態で一覧が描画される |
| GA1a ProductListPage flex box（Gated Amendment 1） | class 欠落 | unit（`ProductListPage.test.tsx` 新規） | GA1a: PageShell root has flex h-full min-h-0 flex-col (no overflow-hidden — round 1 P2-5) | root class にいずれかが欠ける、または `overflow-hidden` が誤って付く |
| GA1b ListShell scroll 箱 + data-list-scroll-container（Gated Amendment 1） | box 欠落 / 属性欠落 / Pagination が box 内 | unit（`ListShell.test.tsx` 新規、`stickyHeader` 分岐） | GA1b: box element has min-h-[12rem] flex-1 overflow-auto, data-list-scroll-container, and data-scroll-restoration-id="products-list"; bottom Pagination is a sibling after the box, not inside it; root has flex-1 min-h-0 (not h-full) | box の class/属性が欠ける、`Pagination` が box の子として見つかる、または root が `h-full` のまま |
| GA1c scroll 解決 helper（Gated Amendment 1） | フォールバック不良 / 複数 scroller / allowlist が main 単独へ退行 | unit（`page-scroll.test.ts` 新規/拡張、fake scroller + `app-router.test.tsx` 拡張） | GA1c: with a `[data-list-scroll-container]` element present, scrollPageToTop / the restoration resolver targets it; without one, falls back to main; querying `[data-list-scroll-container]` returns exactly 1 match on 商品一覧 and exactly 0 matches on a page without the box (paired oracle, round 2 是正 Opus P2 — a lone "at most 1" assertion is vacuously true when 0 boxes exist); `pruneScrollRestorationEntries`'s allowlist and `scrollToTopSelectors` remain the static 2-selector array (main + products-list) across `onBeforeLoad` calls, not collapsing back to main-only (round 3 是正、Opus P2 — the round 1 P1 failure mode: box entry pruned on every `onBeforeLoad`, packet `:255` already requires the static array) | 属性ありでも `main` を対象にしてしまう、属性なしで例外を投げる、`商品一覧` で一致数が 1 でない、箱を持たない画面で一致数が 0 でない、または allowlist/`scrollToTopSelectors` が `onBeforeLoad` を経るたびに `main` 単独へ退行する |
| GA1d 商品一覧 scroll restoration（Gated Amendment 1） | 復元先が main のまま / 横位置が復元されない / scrollLeft mutant が happy-dom の無制限 raw property で生存する / armed 条件が縦のみで横単独ケースを見落とす | unit（`ProductListPage.scroll-restoration.test.tsx` 新規、幾何 stub は `src/lib/app-router.test.tsx:127-158` の `installNativeScrollClamp` を**拡張**——round 2 是正 Opus P1: `scrollTop`/`scrollHeight`/`clientHeight` の clamp に加え、同型の `scrollLeft`/`scrollWidth`/`clientWidth` の clamp setter を追加する。happy-dom 20.10.6 の `scrollLeft`〈`node_modules/happy-dom/lib/nodes/element/Element.js:165-174`〉は無制限の raw property で router-core 内蔵の復元〈`scroll-restoration.js:147`〉が先に代入するため、幅の clamp が無いとアプリ層の `scrollLeft` 復元行を削除しても pass してしまう。**round 3 是正（Opus P2）**: `scrollWidth` は固定値ではなく `scrollHeight` stub（`:136-141`、content marker 有無で 500/1,000）と同型の条件付き getter にする——content marker 不在時は `clientWidth`、出現後は保存値 + `clientWidth` 以上を返す。固定 `scrollWidth` だと横判定が初回から常に満たされ、`scrollLeft` 復元行を削除しても pass する mutant が生存する。round 1 P2-6: `OperationLogsPage.scroll-restoration.test.tsx` の `scrollTop===0` oracle は幾何 stub を持たず `applyWhenScrollable`〈`app-router.ts:105`〉を通過しないため参考にならない） | GA1d: box scrollTop and scrollLeft are both restored on 一覧→詳細→戻り for 商品一覧, verified against a width-clamped `scrollLeft` (not happy-dom's raw unclamped property), including the single-axis cases (saved scrollY=0 with scrollX>0 restores horizontally only, and vice versa) | 復元が `main.scrollTop` に対して行われ box の位置が戻らない、`scrollLeft` が復元されない、`scrollLeft` 削除 mutant が幅 clamp なしの harness で生存する、または保存 scrollY=0・scrollX>0 のケースで復元されない（armed が縦のみの mutant） |
| GA1e stickyHeader/page root 高さ連鎖 fs scan（Gated Amendment 1、round 2 是正 Sonnet P2 で新設） | height-chain 未配線のまま `stickyHeader` 配線が増える | unit（既存 fs scan test `src/test/page-root-pageshell-sweep.test.ts` 拡張、round 1 item 7） | GA1e: every `*Page.tsx` file that passes `stickyHeader` to `ListShell` also passes `flex h-full min-h-0 flex-col` (or equivalent height-chain classes) to its `PageShell` in the same file (limitation: same-file string scan only — see packet `:226` for the cross-file gap) | `stickyHeader` を渡す画面が同一 file 内で height-chain class を `PageShell` に渡していない |
| GA2 PriceRevisionFilters 取引先グルーピング（Gated Amendment 2） | unit 分離で折返し時に離れる | unit（`PriceRevisionFilters.test.tsx` 新規） | GA2: 取引先 label/Select/Button share one flex wrapper div (primary oracle); DOM order Select→Button→(...)→表示件数 Select is preserved (regression guard, not the primary cause per 起票時実測) | 3 要素が共通 wrapper を持たない、または DOM 順序が崩れる |
| GA3a-1 商品コード td/th 内 div の固定幅（Lane 4 Gated Amendment 3、owner L3 run 2 実機観測で機序確定。round 1 是正 Opus P1-2: td/th 自体の min-w/max-w ではなく内側 div で幅確定） | div の幅/wrap class 欠落 / `w-28` 残存 | unit（`ProductTable.test.tsx` 拡張、既存 `:114-118`〈td を取得する既存 SC9d〉を div 取得 + class 確認へ書換え、th 側は新規） | GA3a-1: product code td's inner div carries w-32/whitespace-normal/break-all; product code th's inner div carries w-32; outer td/th carry min-w-36 (belt) only, no max-w-36; `rg -Fn "w-28" src/features/products/components/ProductTable.tsx` = 0 | div の `w-32`/`whitespace-normal`/`break-all` のいずれかが欠落する、th 側 div の `w-32` が欠落する、`w-28` が file 内に残存する |
| GA3a-2 識別列 2 sticky offset 更新（Lane 4 Gated Amendment 3、round 1 是正 Opus P1-3: 3rd site 追加） | offset 未更新 / 新旧混在 | unit（`ListShell.test.tsx` 拡張〈既存 `:462,465`〉+ `ProductListPage.test.tsx` 拡張〈既存 `:667`、3rd site〉の `left-[7rem]` assertion を置換） | GA3a-2: `IDENTITY_COLUMN_CLASSES[2]` 由来の root token contain `left-[9rem]` for both thead th:nth-child(2) and tbody td:nth-child(2); `rg -Fc "left-\[7rem\]" src/` = 0 (repo-wide, not ListShell.tsx-only) | `left-[9rem]` が欠落する、または `left-[7rem]` が `src/` のいずれかの file に残存する（`ProductListPage.test.tsx:667` の見逃しを含む） |
| GA3a-3 div 幅/offset の parity oracle（単一 source、Lane 4 Gated Amendment 3、round 1 是正で div wrapper 前提へ更新） | 片方だけ変更される mutant | unit（`ListShell.test.tsx` 新規） | GA3a-3: the rem value parsed from `PRODUCT_CODE_CELL_WRAPPER_WIDTH_CLASS`'s `w-32` (8) plus td/th padding (1) equals the rem value parsed from `left-[9rem]` in `IDENTITY_COLUMN_CLASSES[2]` (9) | div 幅+padding と offset の rem 値が不一致になっても検出されない |
| GA3a-4 w-28 消滅チェックの file scope（Lane 4 Gated Amendment 3、空集合 oracle 衝突の回避） | 他 4 file への誤爆 / repo 全体チェックの vacuous 成立 | unit（`ProductTable.test.tsx` 拡張、対の fs oracle） | GA3a-4: `w-28` occurs exactly 0 times in `ProductTable.tsx` while `DisposalPage.tsx`/`InventoryRecordsPage.tsx`/`ReceivingPage.tsx`/`IntegrityCheckPage.tsx` each still contain their pre-existing `w-28` occurrences (paired oracle, not a repo-wide 0-count) | `ProductTable.tsx` の `w-28` が消えない、または他 4 file の正当な `w-28` が誤って 0 件と扱われる |
| GA3b-1 ProductListPage 高さ連鎖の撤去（Lane 4 Gated Amendment 3、GA1a の逆転） | 撤去漏れ | unit（`ProductListPage.test.tsx` 拡張） | GA3b-1: PageShell root carries none of flex/h-full/min-h-0/flex-col | `PageShell` root にいずれかの高さ連鎖 class が残る |
| GA3b-2 ListShell stickyHeader root flex 撤去（Lane 4 Gated Amendment 3、GA1b の逆転） | 撤去漏れ / space-y-3・list-shell-sticky・STICKY_TABLE_CLASSES・identityColumns token の巻き添え削除 | unit（`ListShell.test.tsx` 拡張） | GA3b-2: stickyHeader root carries none of flex/flex-1/min-h-0/flex-col, while `space-y-3`/`list-shell-sticky`/STICKY_TABLE_CLASSES/identityColumns-derived tokens are unchanged | flex 系 class が残る、または無関係な token が巻き添えで消える |
| GA3b-3 箱 class の max-h 化（Lane 4 Gated Amendment 3） | flex-1/min-h-[12rem] 残存 / max-h 欠落 | unit（`ListShell.test.tsx` 拡張） | GA3b-3: box element carries `max-h-[168rem]`（最終決定値）and `overflow-auto`, and carries neither `min-h-[12rem]` nor `flex-1`; `data-list-scroll-container`/`data-scroll-restoration-id="products-list"` unchanged | `min-h-[12rem]`/`flex-1` が残る、`max-h-[...]` が欠落する |
| GA3b-4 toolbar shrink-0 の除去（Lane 4 Gated Amendment 3、補助） | 死んだ class の残存 | unit（`ListShell.test.tsx` 拡張） | GA3b-4: toolbar element does not carry `shrink-0` | `shrink-0` が残る |
| GA3b-5 GA1e fs scan の反転（Lane 4 Gated Amendment 3、round 1 是正 Sonnet P3: oracle を具体化） | 反転漏れで既存 GA1e が red のまま / 曖昧な「suite が通る」判定 | unit（`page-root-pageshell-sweep.test.ts` 拡張、GA1e を反転。削除は選ばない——反転の方が「意図的に高さ連鎖を持たない」ことを明示的に守れる） | GA3b-5: `page-root-pageshell-sweep.test.ts` asserts that every `*Page.tsx` file passing `stickyHeader` to `ListShell` does NOT pass `flex`/`h-full`/`min-h-0`/`flex-col` to its `PageShell` in the same file (inverted from GA1e's original positive assertion); concretely, `ProductListPage.tsx` must appear in the violations-would-be list under the OLD assertion and must NOT appear under the NEW (inverted) assertion | 是正後も `ProductListPage.tsx` が height-chain class を持つ扱いのまま（反転が実際には効いていない）、または assertion 自体が widened されて何を確認しているか不明瞭になる |
| GA3b-6 scroll 復元の main/箱 二元化（Lane 4 Gated Amendment 3、round 1 是正 Opus P2: armed/disarm 条件表を明記） | main の縦復元だけ実装される / 箱の横（+51行超時の縦）復元が落ちる / main と箱を同時に scroll した状態から片方しか戻らない | unit（`ProductListPage.scroll-restoration.test.tsx` 拡張、GA1d の 3 test を拡張し main 側の armed/適用ケースを追加。**新規**: main を縦に、箱を横に、それぞれ独立に scroll した状態から一覧→詳細→戻りで両方が同時に復元されるケース） | GA3b-6: `<main>` restores its own vertical scroll independently of the box, and the box continues to restore horizontal scroll always plus vertical scroll when content exceeds 50 rows; when both `<main>` (vertical) and the box (horizontal) are scrolled simultaneously before navigating away, both restore independently on return | main の縦復元が実装されない、または箱の横/条件付き縦復元が退行する、または main/箱を同時に scroll したケースで片方だけ復元される（disarm の合成 stop 漏れ） |
| GA3b-7 forced-top の main/箱 両対応（Lane 4 Gated Amendment 3、round 1 是正 Opus P2: 現行行番号 `app-router.ts:92-95`/`:52-58` へ更新） | 片方にしか効かない | unit（`app-router.test.tsx` 拡張） | GA3b-7: forced-top (`app-router.ts:92-95` and `applyMainNavScroll` `:52-58`) resets both `<main>`'s vertical position and the box's horizontal position (and the box's vertical position when it is the scroller, i.e. >50 rows) | main または箱のいずれか一方だけがリセットされる |
| GA3b-8 scrollPageToTop の main/箱 両対応（Lane 4 Gated Amendment 3、round 1 是正 Opus P2 新設） | `??` 択一のまま片方にしか効かない | unit（`page-scroll.test.ts` 新規/拡張） | GA3b-8: `scrollPageToTop()` (`page-scroll.ts:50-59`) calls `scrollTo` on both `<main>` and the box when both exist in the DOM (not the current `??` either-or) | 箱が存在するとき main 側の `scrollTo` が呼ばれない（現行 `??` の退行） |
| GA3b-9 高さ連鎖コメントの是正（Lane 4 Gated Amendment 3、round 1 是正 Opus P2） | stale コメント残存 | fs scan（`rg -Fc "高さ連鎖" src/`、既存 GA1e/GA1a コメントの is-scan 拡張ではなく独立 grep 完了条件） | GA3b-9: `rg -Fc "高さ連鎖" src/` = 0（起票時点 baseline 4: `ListShell.tsx:12`、`ProductListPage.tsx:259`、`page-root-pageshell-sweep.test.ts` 2 箇所） | `ListShell.tsx`/`ProductListPage.tsx` の GA1 コメントが「stickyHeader は page root の高さ連鎖とセット」という GA3b 実装後は誤りになる説明のまま残存する |

## Mutation Oracle Notes

- SC1a/SC1b/SC1c は 3 件で 1 組の対照 oracle（0 件相当 null / 複数ページ表示 / 境界 51 件 2 ページ目の有効「前へ」）。`totalPages<=1` の null 化だけを見る単独 test は「常に null を返す」mutant を通すため、SC1b・SC1c の非 null 期待が必須（空集合 oracle 衝突の回避、SC1a 単独運用禁止）
- SC3a〜SC3g は `totalCount > 0` の分岐でのみ描画されることを、EmptyState 分岐（該当画面の 0 件 test）が既存のまま変化しないことと対で確認する（新規 summary が誤って空状態にも描画されない）。加えて既存の単数一致 `getByText` test 6 箇所の是正漏れ（重複文言のまま放置）を fail 条件に含める
- SC4a〜SC4g は file ごとに旧 class の `rg -Fn` 完全一致文字列を Plan Packet の Scope S1 節から転記し、新旧が同一 test 内で排他であることを確認する
- SC5a は Checkbox の `aria-label`／`htmlFor` 等、既存 assertion を壊さない形で DOM 順のみを rewrite する（新規 test を追加しない）
- SC5b は「配線漏れ」mutant（`onPerPageChange` を渡さない）を既存 SC9a が検出することを前提に、新規に追加するのは DOM 位置（最後尾）の assertion のみとする
- SC9a/SC9b は対の oracle（`identityColumns` あり/なし）。あり側だけの単独 test は「常に固定 class を付ける」mutant を通すため、なし側の非付与期待が必須（空集合 oracle 衝突の回避）
- SC9a は「列 1-2 に付く」「列 3 以降に付かない」「`thead th` に `z-[11]` が付く」の 3 条件を同一 test 内で確認し、列数の過不足・z-index 抜け（Opus P1/P2）を検出する
- SC9c は `w-min min-w-full` の完全一致（既存 `:346` assertion と同じ厳密度）を維持し、識別列固定の実装が wrapper 側へ意図せず波及しないことを保証する。toolbar 側の assertion は行わない（round 3 是正、no-op のため Scope 対象外）
- S3 是正 4 箇所（`ListShell.test.tsx:96,98,107,119`/`ProductListPage.test.tsx:632-637`/`Pagination.test.tsx:71`）は S2 の class 変更が既存 test の判別子（`.text-base`/`toHaveClass("text-base", ...)`）を壊す mutant 検出であり、是正漏れがあれば既存 test 自体が red のまま残ることを oracle とする（round 3 追加）
- GA1b は「box の class が欠ける」と「`Pagination` が box の中に入ってしまう」の 2 種の mutant を区別して検出する（前者は class 文字列の欠落、後者は DOM 構造の逸脱で、どちらも「下部ページ送りが常に見える」という owner 期待像を壊す別々の failure mode）
- GA1c は fake scroller の有無で対照 oracle を作る（属性ありのときだけ helper がそれを対象にする、無いときは `main` にフォールバックする、の両方を同一 test 内で確認し「常に `main` を返す」「常に fake scroller を返す」のどちらの mutant も検出する）。加えて `[data-list-scroll-container]` の一致数を対の oracle にする（round 2 是正、Opus P2）: 商品一覧では**ちょうど 1**、箱を持たない画面では**ちょうど 0**——「高々 1 個」の単独 assertion は箱が存在しない画面でも常に真になり vacuous に成立するため、0 件側の対照ケースを必須にする（round 1 P3-8 の意図＝2 個目が生まれても文書順先頭を無警告採用しないことの保証、は 1 件側で維持）。**round 3 是正（Opus P2）**: `pruneScrollRestorationEntries`/`scrollToTopSelectors` が静的 2-selector 配列のまま複数回の `onBeforeLoad` を跨いで不変であることも確認する——round 1 P1 が指摘した「box entry が毎 `onBeforeLoad` で削られる」退行を、resolver 化ではなく静的配列という是正後の形で再度検出できるようにする
- GA1d は縦横それぞれ単独の armed ケースを oracle に含める（round 3 是正、Opus P2）: 保存 `scrollY=0`・`scrollX>0` で横のみ復元される、およびその逆（保存 `scrollX=0`・`scrollY>0` で縦のみ復元される）の両方を確認し、armed 条件が縦のみ・横のみに偏った mutant を検出する
- GA1e は round 1 item 7（`stickyHeader`/page root 高さ連鎖の regression guard）が Design 節の散文としてのみ存在し、Test Design Matrix・Contract Coverage Ledger に対応行が無かった欠落を埋める（round 2 是正、Sonnet P2）。fs scan の性質上、`stickyHeader` の配線と `PageShell` の height-chain 配線が同一 file にある前提でのみ機能する限界を Test Name に明記する
- GA2 は共通 wrapper の存在を主たる oracle、DOM 順序（`compareDocumentPosition`）を回帰ガードとして両方確認する。owner 所感の原因は `flex-wrap` による折り返し分離**および**一様 `gap-3` による群化の欠如であり（round 1 P3-9、`gap-3`/`gap-2` の詰まりも同等以上に効く）、DOM 順序は起票時実測で既に正しいと判明しているため、共通 wrapper の存在確認が本 Gated Amendment の主たる oracle、DOM 順序確認は既存契約の回帰ガード（round 1 P3-10: rg 件数条件は無関係な編集でも満たせるため主たる完了条件から降格、補助のみ）
- GA3a-1〜GA3a-3 は幅と offset の一致という 1 つの契約を「幅 class の存在」「offset class の存在」「両者の rem 値一致（parity）」の 3 段に分けて確認する。parity test（GA3a-3）が主たる oracle——存在確認だけでは「幅を変えたが offset を変え忘れる」mutant（またはその逆）を見逃す。GA3a-4 は空集合 oracle 衝突を避けるための対 oracle（memory rule「空集合の期待値は非空 case と対にする」に従う）: `ProductTable.tsx` の `w-28` が 0 件になったことだけを見る単独 assertion は、`rg` の対象パスを誤って repo 全体へ広げる mutant（他 4 file の正当な `w-28` も巻き込んで消してしまう実装ミス）を検出できない。他 4 file の `w-28` が変わらず残っていることを同一 test 内で確認し、「対象 file だけを狭く直せているか」を担保する
- GA3b-1〜GA3b-5 は GA1a/GA1b/GA1e の逆転であり、「反転漏れ（旧 class が残る）」と「巻き添え削除（無関係な token まで消える）」の 2 種の mutant を区別して検出する。GA3b-2 は特に `space-y-3`/`list-shell-sticky`/`STICKY_TABLE_CLASSES`/`identityColumns` 由来 token が無変更であることを同一 test 内で確認し、flex 系 class の削除が識別列固定（S9）や sticky thead の契約を巻き込まないことを保証する
- GA3b-6〜GA3b-8 は GA1d のパターンを踏襲し、main（縦）と箱（横・条件付き縦）を独立した対の oracle にする——「main の復元だけ実装して箱を退行させる」「箱の復元だけ実装して main を実装し忘れる」のどちらの mutant も見逃さない。GA3b-6 は両 target を同時に scroll したケースを追加し、disarm を合成 stop で行わず片方の observer を上書きしてしまう mutant（round 1 是正、Opus P2）を検出する。GA3b-8 は `scrollPageToTop()` の `??` 択一残存を検出する対 oracle（round 1 是正、Opus P2 新設）

## Contract Coverage Cross-check

Plan Packet の Contract Coverage Ledger と 1:1 対応する。SC9〜SC7・GA1a〜GA2・GA3a〜GA3b はすべて vitest（happy-dom の class/DOM 順・scroll 値検査）。実際の視覚的な「識別列が固定されたまま右側だけ流れる」「取引先ボタンが隣にある」「箱が 50 行分の高さまで伸びる／51 行超で scroll する／見出しが page scroll 中に流れる」見え方は happy-dom で判定できないため、AC-L3-1〜AC-L3-4（run 3）の owner Windows native L3 が唯一の oracle（Lane 3/5 の docs-review 先例とは異なり、本 lane に docs-only の静的 oracle は無い — DSR-17/DSR-22/catalog ⑩/⑯/⑰ の文言同期は Plan Packet Scope S6〜S8・Gated Amendment 1 で `rg -Fn`/`rg -c` 完全一致検査を行う。`rg -c` は一致した行数を数える点に注意。GA3b の DSR-17/DSR-22/catalog ⑯ 同期は owner 判定〈run 3〉確定後に別途行い、本 Gated Amendment の Scope には含めない）。AC-L3-4 は PASS/FAIL の合否条件ではなく owner が (a)/(b)/(c) を選ぶための判定材料であり、GA3b-1〜GA3b-7 の機械検査（class/DOM/scroll 値）とは別に、owner Windows native L3 run 3 でのみ確認する（Contract Coverage Ledger の L3 列は「AC-L3-4（run 3、判定材料）」と明記し、機械検査の PASS/FAIL 項目とは区別する）。
